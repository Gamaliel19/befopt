# Copiez ce fichier en ".env" (jamais commité dans Git) et remplissez les
# valeurs réelles. En local (Windows), lancez le serveur avec :
#   set DJANGO_SETTINGS_MODULE=config.settings.development
# (déjà la valeur par défaut de manage.py, donc pas obligatoire en local)

# Générez une vraie clé avec :
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=_j8^!y1z(l9lhkh93c4!=y(!9)w%*0zw31d#4m+@27dbsjj-xn

DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Base de données PostgreSQL locale (Phase 4 : à installer sur Windows).
DB_NAME=befopt
DB_USER=befopt
DB_PASSWORD=changez-moi
DB_HOST=localhost
DB_PORT=5432

# Formulaire de contact.
DEFAULT_FROM_EMAIL=contact@befopt.td
CONTACT_RECIPIENT_EMAIL=deubarodrigue2018@gmail.com

# Utilisés uniquement par config/settings/production.py, en production.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
